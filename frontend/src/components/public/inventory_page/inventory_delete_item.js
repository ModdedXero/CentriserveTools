import React from "react";
import Modal from "../../utility/modal";

export default function InventoryDeleteItem({location, category, item, itemName, modal, setModal}) {
    return (
        <Modal visible={modal} onClose={setModal}>
            
        </Modal>
    )
}